export type Task = {
    id: string
    text: string
    done: boolean
}

export type Vegetable = {
    id: string,
    title: string,
    description: string,
    backgroundColor: string,
    available: boolean,
}
